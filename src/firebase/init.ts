import '@react-native-firebase/app';

// Import any native Firebase module you need here so it is initialized
// when the app starts.
import '@react-native-firebase/firestore';

export default function initFirebase() {
  return null;
}
